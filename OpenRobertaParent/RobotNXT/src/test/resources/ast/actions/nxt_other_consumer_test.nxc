#define WHEELDIAMETER 5.6
#define TRACKWIDTH 12.0
#define MAXLINES 8 
#include "NEPODefs.h" // contains NEPO declarations for the NXC NXT API resources 
 


task main() {
    OnFwd(OUT_A, SpeedTest(30));
    RotateMotor(OUT_A, SpeedTest(30), 360 * 1);
    RotateMotor(OUT_A, SpeedTest(30), 1);
    Float(OUT_A);
    Off(OUT_A);
}


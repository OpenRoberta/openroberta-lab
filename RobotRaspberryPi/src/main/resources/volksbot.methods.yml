include: "classpath:/common.methods.yml"

LIGHT:
  PYTHON: |
    def _light(pixels, num):
      for j in range(0, num):
        for i in range(0,12):
          pixels[i - 3] = (0, 0, 0)
          pixels[i - 2] = (0, 80, 0)
          pixels[i - 1] = (0, 160, 0)
          pixels[i] = (0, 255, 0)
          time.sleep(0.2)
      pixels.deinit()
